import React from 'react';

let headStyle = {
    backgroundColor: 'blue',
    height: '30px',
    width: '30px',
    margin: 'auto'
}

const SnakeHead = (props) => {
    return (
        <div className='snakeHead' style={ headStyle }>

        </div>
    );
};

export default SnakeHead;